# ============================================
# BAD PROPERTIES FILE ? For testing purposes
# ============================================

# ---------- Duplicate Keys ----------
db.url=jdbc:mysql://localhost:3306/testdb
db.url=jdbc:mysql://localhost:3306/productiondb   # Duplicate key

app.name=MyApplication
app.name=MyAppRenamed                             # Another duplicate

# ---------- Illegal Characters & Formatting ----------
# Key contains spaces (illegal)
invalid key name = value

# Key contains colon (illegal)
illegal:key=value

# Missing key-value separator (no '=' or ':')
orphan line without separator

# Unescaped special characters in value (backslashes)
unescaped.string=This contains unescaped \n and \t and a backslash \

# ---------- Unused Keys ----------
# These keys are present but NOT referenced in the code
unused.key.one=This is not used
unused.key.two=Another unused value
deprecated.setting=true

# ---------- Valid Keys (for reference) ----------
valid.db.username=root
valid.db.password=secret

# ---------- Missing Keys (referenced in code but absent) ----------
# The code expects these keys, but they are missing:
# server.port
# app.version
# logging.level.root